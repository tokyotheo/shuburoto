<%= render :partial => 'shared/account' %>
<h1>Users(<%=h @users.size %>)</h1>
<h2><%= link_to 'Lectures', admin_lectures_path %></h2>
<h2><%= link_to 'Lessons', admin_lessons_path %></h2>

<table>
  <tr>
    <th>User</th>
    <th>Email</th>
    <th>Name</th>
    <th>Admin</th>
  </tr>

<% for user in @users %>
  <tr>
    <td><%=h user.user_id %></td>
    <td><%=h user.email %></td>
    <td><%=h user.name %></td>
    <td><%=h user.admin %></td>
    <td><%= link_to '管理ページ', admin_user_path(user) %></td>
<!--
    <td><%= link_to 'Edit', edit_admin_user_path(user) %></td>
    <td><%= link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete %></td>
-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_admin_user_path %>
